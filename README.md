# DeepSign
Efficient deep learning model that can be used to predict sign language to narrow the communication gap between speech-impaired and non-speech-impaired people in the community

Collected key points saved as Numpy arrays using Mediapipe holistic and trained a deep neural network with LSTM layers for sequences to make real time sign language detection using OpenCV.
